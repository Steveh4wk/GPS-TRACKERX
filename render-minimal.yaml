services:
  - type: web
    name: gps-trackerx-minimal
    env: python
    buildCommand: pip install flask gunicorn
    startCommand: gunicorn app_minimal:app
    plan: free
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"